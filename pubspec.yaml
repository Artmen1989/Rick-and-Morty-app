name: rick_and_morty_app
description: A Flutter app for Rick and Morty characters
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0  # ✅ Поддерживает веб
  provider: ^6.0.5  # ✅ Поддерживает веб
  # shared_preferences: ^2.2.2  # ❌ Уберите пока что - проблемы с вебом
  # cached_network_image: ^3.3.0  # ❌ Уберите пока что - проблемы с вебом

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true